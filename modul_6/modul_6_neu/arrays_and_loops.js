let myList = [12, "Banana", true];

let fruits = ["Banana", "Orange", "Apple", "Mango"];

let singleTestVar = fruits.pop(); //pop nimmt das letzte aus array raus

function initArrays() {
    myList[0] = "No Banana";

    console.log(fruits);
}